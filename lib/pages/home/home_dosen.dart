import 'package:flutter/material.dart';

class HomeDosen extends StatelessWidget {
  const HomeDosen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(appBar: null, body: Text("Ini data home dosen"));
  }
}
