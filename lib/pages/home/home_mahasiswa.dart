import 'package:flutter/material.dart';

class HomeMahasiswa extends StatelessWidget {
  const HomeMahasiswa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("Ini data home mahasiswa"));
  }
}
